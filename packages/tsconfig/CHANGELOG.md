# Changelog

All notable changes to `@nexode/tsconfig` will be documented here.

## 0.1.0

- Initial shared TypeScript configurations.
- Added `base.json`.
- Added `node.json`.
- Added `browser.json`.
- Added `react.json`.
